Place the local browser libraries in this folder:
- qrcode.min.js
- jspdf.umd.min.js

The application intentionally references local files rather than external CDN URLs so the QR/PDF functionality can remain self-contained after the page and libraries are available locally.

For deployment, use current official releases of the selected libraries and keep their licenses/notices with the project.
